# Mini POS System

Mini POS System - це проста касова програма на Python і Tkinter.

## Можливості

- додавання товарів;
- генерація EAN-13 штрихкоду;
- створення PNG картинки штрихкоду;
- пошук товару по штрихкоду;
- додавання товарів у чек;
- видалення товару з чеку;
- підрахунок суми;
- знижка у відсотках;
- завершення продажу;
- збереження товарів у `data/products.json`;
- збереження історії продажів у `data/sales.json`.

## Структура

```text
mini-pos-system/
├── main.py
├── products.py
├── barcode_tools.py
├── storage.py
├── sales.py
├── README.md
├── data/
│   ├── products.json
│   └── sales.json
└── barcodes/
```

## Встановлення

Tkinter входить у стандартний Python.

Для генерації справжніх PNG штрихкодів встановіть бібліотеки:

```bash
pip install python-barcode pillow
```

Якщо бібліотеки не встановлені, програма все одно працює, але замість картинки створює файл-заглушку.

## Запуск

```bash
python main.py
```

## Як користуватись

1. У вкладці `Товари` введіть назву, ціну та кількість.
2. Натисніть `Generate barcode`, щоб отримати унікальний EAN-13 код.
3. Натисніть `Add product`, щоб додати товар і зберегти його.
4. У вкладці `Каса` введіть або відскануйте штрихкод.
5. Натисніть `Add to receipt`.
6. За потреби введіть знижку.
7. Натисніть `Finish sale`, щоб завершити продаж.

## Гілка

Робота виконана у гілці:

```bash
feature/barcodes-storage
```
